<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create New Trainer</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <h1>Create New Trainer</h1>
  <form action="<%= formAction %>" method="POST" id="trainerForm">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value="<%= trainer.name || '' %>" required />
    <label for="age">Age:</label>
    <input type="number" id="age" name="age" value="<%= trainer.age || '' %>" min="1" required />
    <label for="gender">Gender:</label>
    <select name="gender" id="gender">
      <option value="M" <%= trainer.gender === 'M' ? 'selected' : '' %>>Male</option>
      <option value="F" <%= trainer.gender === 'F' ? 'selected' : '' %>>Female</option>
      <option value="N" <%= trainer.gender === 'N' ? 'selected' : '' %>>Nonbinary</option>
    </select>
    <label for="bio">Bio:</label>
    <textarea id="bio" name="bio" rows="4"><%= trainer.bio || '' %></textarea>
    <button type="submit"><%= submitLabel %></button>
  </form>
</body>
</html>